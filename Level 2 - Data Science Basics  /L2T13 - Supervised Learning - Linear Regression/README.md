## Practical Task 1: Insurance Cost Prediction (Linear Regression)

This task involves using linear regression to explore the relationship between age and insurance costs, including data visualization.

### Instructions:

* Create a new Jupyter notebook named `insurance_regression.ipynb`.
* Complete the following steps within this notebook.

### Requirements:

1.  **Load Data:**
    * Import `insurance.csv` into your notebook. (Ensure `insurance.csv` is in the same directory or provide the correct path).

2.  **Visualize Age vs. Charges (Scatter Plot):**
    * Plot a scatter plot with 'age' on the x-axis and 'charges' on the y-axis.

3.  **Linear Regression Model:**
    * Use `linear_model.LinearRegression()` from `sklearn` to fit a linear regression model to your data.
    * Make predictions on the data using the fitted model.

4.  **Visualize Best-Fit Line:**
    * Plot another scatter plot (age vs. charges) and overlay the best-fit line generated by your linear regression model.

---

## Practical Task 2: Diabetes Progression Prediction (Multiple Linear Regression)

This task focuses on building a multiple linear regression model to predict diabetes progression, including data preparation steps like splitting and scaling.

### Instructions:

* Create a new Jupyter notebook named `diabetes_regression.ipynb`.
* Complete the following steps within this notebook.

### Requirements:

1.  **Load Data:**
    * Read `diabetes_dirty.csv` into your Jupyter Notebook. (Ensure `diabetes_dirty.csv` is in the same directory or provide the correct path).

2.  **Differentiate Variables:**
    * Differentiate between the independent variables (features) and the dependent variable (target).
    * Assign the independent variables to `X` and the dependent variable to `Y`.

3.  **Train-Test Split:**
    * Generate training and test sets comprising 80% and 20% of the data respectively.

4.  **Data Scaling/Normalization:**
    * Investigate the necessity for scaling or normalization of the data (e.g., by checking distributions or ranges of features).
    * Employ `MinMaxScaler` and `StandardScaler` from `sklearn.preprocessing` if necessary.
    * Fit these scalers on the training set.
    * Apply the fitted scalers to transform both the training and test sets accordingly.

5.  **Multiple Linear Regression Model:**
    * Generate a multiple linear regression model using the training set.
    * Use all of the independent variables (`X`).

6.  **Print Model Parameters:**
    * Print out the intercept and coefficients of the trained model.

7.  **Generate Predictions:**
    * Generate predictions for the test set using your trained model.

8.  **Compute R-squared:**
    * Compute the R-squared score for your model on the test set. You can use `r2_score` from `sklearn.metrics` to obtain this score.

9.  **Comments and Notes:**
    * Ensure your Notebook includes comments about what your code is accomplishing.
    * Add notes (in markdown cells) about model outputs, such as the R-squared score and its interpretation.
  
  ### Libraries:

  * `seaborn`
  * `numpy`
  * `pandas`
  * `matplotlib.pyplot`
  * `sklearn`
